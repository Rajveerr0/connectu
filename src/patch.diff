diff --git a/src/App.jsx b/src/App.jsx
index 1234567..abcdef0 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -1,6 +1,8 @@
 import React from 'react';
 import { BrowserRouter as Router, Routes, Route, useLocation, Navigate } from 'react-router-dom';
 import { motion, AnimatePresence } from 'framer-motion';
+import { ToastContainer } from 'react-toastify';
+import 'react-toastify/dist/ReactToastify.css';
 import { AuthProvider, useAuth } from './context/AuthContext';
 import Header from './components/Header';
 import Footer from './components/Footer';
@@ -18,6 +20,7 @@ import AlumniDashboard from './pages/alumni/AlumniDashboard';
 // Protected Route Component
 import Dashboard from './pages/Dashboard';
+import { notifyWarning } from './utils/notify';

 const ProtectedRoute = ({ children, requiredRole }) => {
   const { user, isAuthenticated } = useAuth();
@@ -26,7 +29,11 @@ const ProtectedRoute = ({ children, requiredRole }) => {
   if (!isAuthenticated) {
-    return <Navigate to="/login" replace />;
+    // Toast instead of browser alert
+    notifyWarning('Please log in to access this feature.');
+
+    return <Navigate to="/login" replace />;
   }

   if (requiredRole && user.role !== requiredRole) {
@@ -127,6 +134,11 @@ function AppContent() {
       <Footer />

+      {/* Toast notifications root container */}
+      <ToastContainer />
+
    </div>
  );
 }
